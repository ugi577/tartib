import { describe, expect, it } from 'vitest';

// Smoke test scaffold — test nyata menyusul di Batch A (porsi, rumusQty).
describe('scaffold', () => {
  it('vitest berjalan', () => {
    expect(1 + 1).toBe(2);
  });
});
