# SESI 22 — catatan state berjalan (2026-09-08)

> Berkas ini = jaring pengaman bila sesi agen terputus. Dibaca setelah PROJECT-STATE.md.
> **Titik lanjut presisi ada di bagian "LANJUTKAN DARI SINI".**

## Arahan Ahmed (kutipan)

- "banyak sekali kekacauan di proyek ini, spt header di beberapa menu terlalu besar, fungsi copi paste dll di struktur organisasi, print privew yg tdk mengikuti ukuran kertas sebenarnya, aplikasi ini jg akan direkomendasikan dr menu studio print di proyek mahad app v3 selain defaultnya berdiri sendiri untuk siapapun yg instal"
- "ingat sll amankan proses yg brjalan karena bisa berhenti tiba2 krn limit agen, lanjut"
- "amankan pekerjaan sblm limit"

## Status langkah

- [x] Baseline: tsc/lint bersih, vitest 266/266, dev server :3000 (pid 36622 — **RUSAK**: `.next/` ditimpa `next build` 7 Sep 13:41 → chunk klien 404, halaman tidak terhidrasi; **restart `pnpm dev` sebelum verifikasi browser**)
- [x] Checkpoint WIP Ahmed: `a433b56 wip(kbm)`
- [x] Audit multi-agent (workflow `audit-kekacauan-tartib`, run `wf_373059a8-db6`): **99 temuan** dari 6 pencari — ringkasan lengkap tersimpan di
  `~/.claude/projects/-Users-ahmad-Projects-tartib-app/2f2c16cc-f1da-4f7f-ae86-58104a1eafd7/tool-results/b98sj574w.txt`
  (jurnal: `.../subagents/workflows/wf_373059a8-db6/journal.jsonl`). Fase verifikasi/sintesis mungkin belum selesai — hasil akhirnya opsional, temuan mentah sudah dibaca semua.
- [x] `94e3b97` modul kertas bersama `lib/cetak/kertas.ts` (+8 test) & `lib/useUkuranElemen.ts`
- [x] Fondasi sesi 22 (commit setelah `94e3b97`): HeaderView + `KELAS.subNav*`, klip bertipe (`simpanKlip/ambilKlipTipe/useKlip`; API lama masih ada sebagai jembatan), `UkuranKertas = IdKertas`, hack nama PIC dihapus dari `sopService`, `buatId` fallback. tsc/lint bersih, vitest 277/277.
- [x] `2814a4b` cetak mengikuti kertas (TartibHost 'kanvas', standaloneHost suntik @page + body.cetak-lembar, globals.css tanpa @page bersarang, PrintReadyCanvas mm tetap + fit-to-width + slot toolbar, CanvasHubView kop Pengaturan + tiket nyata)
- [x] `1afaa34` shell & header: page.tsx (nav konsisten, 10 kartu beranda, ?dok=, onBuka, ⌘K, aria), HeaderView di Pengaturan/Acara/Template/Tamu/Evaluasi/e-Konfirmasi, kartuIsi, seed tanpa nama
- [x] `fb669ee` merge Agen B (AppDialog/ContextMenu portal + keyboard + anchor, PemilihIkonManual token) — 285 tes
- [ ] Agen A (KBM), C (printer/cari/identitas), D (preset), E (struktur) masih berjalan di worktree `.claude/worktrees/agent-*` — cabang: lihat `git worktree list`; merge ke master satu per satu dengan `git merge --no-ff <cabang>` lalu tsc/lint/vitest
- [ ] Setelah semua merge: hapus shim `SopView as unknown as` di page.tsx (SopView sudah punya prop onBuka), hapus API lama di appClipboard.ts, sesuaikan pemanggil ContextMenu agar mengirim `anchor` dari tombol ⋯
- [ ] Verifikasi browser 375px + 1280px (restart dev dulu)
- [ ] Perbarui PROJECT-STATE.md + PLAN.md changelog v1.31 + DECISIONS K-26

## Temuan audit terpenting (99 temuan; nomor = urutan di berkas ringkasan)

Angka nyata (headless Chrome, 375×812): kartu jabatan pertama Struktur PIC di **y=509** (kerangka 453px), lembar kanvas di **y=435** dan **menyusut 309px** (flex-shrink, bukan overflow), Preset 255, Pengaturan 208, e-Konfirmasi 204, beranda grid 383. `@page` bersarang di globals.css terbukti **kosong di CSSOM** (hanya `size:auto` yang berlaku).

1. **Cetak** — [28/73/88] @page bersarang tidak valid; [29/45/89] lembar tidak berukuran tetap & tidak fit-to-width; [32/90] empat angka margin berbeda (safe-zone 3,7mm vs label 10mm vs padding 12mm vs @page 10mm); [33] kerangka layar (main pt-14 pb-24, meja kerja abu) ikut tercetak; [34] prefix `sm:` di dalam lembar mengikuti viewport bukan kertas; [35/87] dua sistem kertas (F4 210 vs 215); [36] thermal dipaksa width 100%; [37] '+ Isi' & petunjuk tercetak, tanpa `print-color-adjust`; [38] tanpa `break-inside-avoid`; [40/91] kanvas melewati `host.cetak()`; [43/71] **dialog/toast/context-menu KBM terjebak di lembar ber-transform** (fixed relatif ke kertas) → portal AppDialog & ContextMenu ke body + pisahkan toolbar KBM dari lembar; [30/70] kop hardcode "PONDOK PESANTREN & MADRASAH TARTIB" + tiket dummy "Adrian Maulana"; [31/50/69] printer BT/USB "berhasil dicetak" palsu, baterai fiktif.
2. **Header** — [1/96] Kanvas 4 bar bertumpuk; [2/53/95] PIC Amanah: judul + 2 link duplikat nav + sub-nav + kartu ringkasan 3 tombol; [3/44] toolbar KBM di DALAM kertas (tombol tak terjangkau di HP); [4] banner beranda 303–325px mengulang grid; [5/47] Preset header 157px + filter 4 tombol overflow; [46] pemilih dokumen kanvas overflow; [6] dua pola header & dua idiom sub-nav; [7/76] nama menu tidak konsisten (Preset/Katalog…, PIC/PIC Amanah/Struktur PIC…, Kanvas/Print-Ready Canvas); [8] Pengaturan kartu `p-6`; [9] e-Konfirmasi keterangan 4 baris; [11] `pt-14` vs kontrol melayang 46px.
3. **Copy-paste struktur** — [13/72] potong→tempel = "(Salinan)", ceklis/urutan hilang, sub dari snapshot basi; [14] tanpa transaksi; [15/25/79] klip lintas tipe rusak (`data: any`); [16] mode Daftar → Bagan data basi; [17] jabatan dengan rutin Harian/Mingguan hilang dari bagan; [18/93] shortcut Ctrl+C/X/V/D/Del dipamerkan tanpa handler; [19] tanpa umpan balik/tanda "dipotong"; [20/83] logika duplikat 200+ baris (PanelItemSop vs BaganOrganisasi), SopView 2360 baris; [21/48/49/92] hanya klik kanan — di HP tidak terjangkau, tombol Ubah/+Tugas/Hapus 10px opacity-60; [22] duplikat tanpa transaksi; [23/61/94] ContextMenu tinggi salah, tanpa role menuitem, menimpa nav, kelas no-op; [26] clipboard sistem tanpa catch (sudah dihapus di fondasi).
4. **Umum** — [12/63] hack nama "lutfi→Yudi" (service sudah dihapus; **masih ada di SopView.tsx** muat ×2, bukaUbah, render ×2); [64] "Gunakan Template" menghapus struktur tanpa konfirmasi; [65] preset SOP tidak menerapkan apa pun; [66] `<a href="/?view=…">` mentah → 404 di basePath GitHub Pages (SopView 2097/2103, CanvasHubView 113); [67] view template/evaluasi/konfirmasi tidak terjangkau; [42/57/68] alert/confirm (KbmMatriksView 377/443/470/485, PresetLibraryView 42); [24/75] seed & preset berisi nama pengurus asli; [55/74] `dark:` AKTIF di mode gelap OS di PemilihIkonManual; [77] identitas produk usang (layout.tsx, README, Tentang); [78] HariKbm 'Jumat'/"Jum'at"; [80] komponen baru tidak memakai KELAS; [81] `?view=canvas` selalu buka Struktur (perlu `&dok=kbm`); [82] hapus/reset template KBM salah; [51] modal printer di stacking z-40; [52] nav tanpa aria-label; [58/84] ⌘K tidak membuka pencarian; [85] komentar page.tsx usang, IKON.cari mati; [98] buatId (sudah); [99] dev server rusak.

## LANJUTKAN DARI SINI — rencana eksekusi (strategi: 5 agen worktree paralel + saya)

Kepemilikan berkas DISJOINT (agar bisa paralel di worktree lalu di-merge ke master):

- **Agen A — KBM**: `KbmMatriksView.tsx`, `types/kbm.ts` (HariKbm saja), `lib/presets/presetKbm.ts`, `lib/presets/index.ts`, baru `lib/kbm/hari.ts`(+test). Tugas: alert/confirm → KonfirmasiDialog/FormDialog error; pisahkan toolbar dari lembar (portal toolbar ke `#kanvas-toolbar-slot` yang dirender PrintReadyCanvas, `useEffect` cari slot); kop di kertas bergaya dokumen (tanpa kartu/gradasi); tanpa `sm:` di dalam lembar; '+ Isi' & petunjuk `print:hidden`; `break-inside-avoid` per `<tr>`; footer `jadwal.diubahPada`; normalisasi hari (kanonik 'Jumat', `labelHari` untuk tampilan); hapus template → ganti aktif hanya bila id sama; reset hanya bila `asalId` ada; klip → `simpanKlip({tipe:'kbm',…})`/`ambilKlipTipe('kbm')` + `useKlip`; ringkas toolbar (Template select + Simpan + '⋯' menu); [27] sub yatim di `terapkanPresetStruktur` (hapus sub per sopId).
- **Agen B — dialog/menu/ikon**: `AppDialog.tsx` (createPortal ke body), `ContextMenu.tsx` (portal, ukur tinggi nyata useLayoutEffect, clamp di atas nav ~80px, `role=menuitem`, fokus & panah, item `min-h-[40px]`, shortcut `hidden sm:inline`, prop `anchor?: DOMRect`, hapus kelas no-op animate-in/active:scale-98, palet KELAS terang), `PemilihIkonManual.tsx` (hapus semua `dark:`, token KELAS, target sentuh ≥36px, baris pratinjau flex-wrap).
- **Agen C — printer/pencarian/identitas**: `PrinterStatusBadge.tsx` (modal via AppDialog; BT/USB disabled + keterangan jujur bila API tidak ada; hapus simulasi & baterai fiktif; Uji Cetak → arahkan ke kanvas), `webPrinterService.ts` (BT/USB tidak mengaku sukses → jatuh ke dialog cetak sistem; hapus atribut body; hapus `as any` → interface Navigator), `SearchModal.tsx` (tombol tutup ikon + aria, `pt-3 sm:pt-24`, `max-h-[50dvh]`, hapus listener ⌘K ganda, sumber acara/template atau hapus kategori 'Acara'), `TentangView.tsx` + `README.md` + `layout.tsx` metadata (identitas: "Struktur organisasi, jadwal KBM & SOP acara siap cetak — offline"; KEMAMPUAN sesuai fitur nyata; kartu `KELAS.kartuIsi`).
- **Agen D — preset**: `PresetLibraryView.tsx` (HeaderView judul 'Preset' + SubNav Semua/Struktur/SOP/KBM tanpa emoji; hapus alert → pesan `KELAS.error`; KonfirmasiDialog sebelum terapkan struktur dengan angka nyata "X jabatan & Y tugas akan diganti" via `sopSvc.daftarItemSop/daftarSubItemSop`; KBM konfirmasi bila jadwal aktif `diubahPada`; navigasi ke `canvas` dengan `dok=kbm`), `presetSop.ts` + baru `lib/presets/terapkanPresetSop.ts`(+test) yang membuat template nyata via `templateService.imporTemplate`/`bangunStrukturImpor` (fase dari string + offset, seksi → divisi) lalu navigasi ke 'template'; `presetStruktur.ts` nama pribadi → generik/kosong.
- **Agen E — struktur (copy-paste)**: `SopView.tsx` → pecah ke `components/struktur/{PanelItemSop,BaganOrganisasi,FragmentCetak}.tsx` + `lib/struktur/menuStruktur.ts` (builder menu dipakai keduanya) + hook `usePapanSop(sopId)` (satu sumber items/sub, muat ulang saat kembali dari mode daftar); `sopService.ts` tambah `duplikatItemSop(itemId)` (sisip setelah asli, transaksi), `salinItemKePapan(itemId, sopIdTujuan, {rutin?})`, `pindahkanItem(itemId, {sopIdTujuan, rutin?})` (update, bukan buat baru — ceklis utuh, tanpa "(Salinan)"), `pindahkanSubItem(subId, itemIdTujuan)`, `salinSubItem(subId, itemIdTujuan)` semua dalam `tartibDb.transaction` + test; klip → `simpanKlip`/`ambilKlipTipe`/`useKlip`, menu Tempel disabled bila tipe tak cocok, kartu `id===klip.id && isCut` diberi tanda, pil "Klip: …" + batal; hapus label shortcut (tanpa handler); tombol '⋯' (`KELAS.tombolIkon`, ≥40px) di kartu/baris memanggil menu yang sama (anchor rect); tombol Ubah/+Tugas/Hapus → dalam menu; toggle expand jadi `<button aria-expanded>`; checkbox `h-5 w-5`; rutin di luar tier → grup 'Lainnya'; hapus sisa hack nama (muat ×2, bukaUbah, render ×2, placeholder 'Yudi Nahyuddin'); header SopView → `HeaderView` judul 'Struktur & PIC' + SubNav, tanpa link Preset/Kanvas (`<a href>` mentah dihapus → prop `onBuka`); kartu ringkasan bagan jadi satu baris tipis + '+ Tambah'; kertas SopView → `lib/cetak/kertas.ts` (`KERTAS_LEMBAR`, `aturanPage`, `bacaPilihanKertas`), hapus `UKURAN_KERTAS`/`tartib.sop.kertas`.
- **Saya (master)**: `PrintReadyCanvas.tsx` (dimensi mm tetap dari `dimensiKertas`, padding = margin, safe-zone `inset` = margin mm, fit-to-width via `useUkuranElemen` + wrapper tinggi×skala, tanpa transform saat skala 1, pilihan kertas A4/F4/Letter/Legal/A5/Thermal80/58 + orientasi, `bacaPilihanKertas`, slot `#kanvas-toolbar-slot`, toolbar 2 baris HP, `shrink-0`), `CanvasHubView.tsx` (HeaderView 'Kanvas Cetak' + SubNav dokumen, prop `dokumenAwal` dari `?dok=`, kop dari `usePengaturan`/`barisKop`, tiket = pilih jabatan nyata, `break-inside-avoid`, tanpa `sm:` di lembar, tanggal via `tanggalHariIni`, `onBuka` prop), `globals.css` (hapus @page bersarang; `main` print reset; `print-color-adjust: exact` di lembar; `body.cetak-lembar` sembunyikan semua kecuali `#print-ready-sheet`), `TartibHost.ts` + `standaloneHost.ts` (payload `{jenis:'kanvas', dokumen, judul, kertas, orientasi}` → inject `<style id="tartib-page">` + `afterprint` cabut), `page.tsx` (pt-12, banner beranda ringkas, kartu Template/Evaluasi/e-Konfirmasi kembali, ⌘K listener, aria-label nav, `dok` param, komentar, hapus IKON.cari, nama menu konsisten: Beranda/Preset/Struktur & PIC/Kanvas Cetak/Pengaturan), header view lama → HeaderView (Acara/Template/Tamu/Evaluasi/EKonfirmasi/Pengaturan; Pengaturan/Tentang `kartuIsi`), `seed.ts` nama pribadi → PIC kosong.
- **Setelah merge semua**: hapus jembatan API lama di `appClipboard.ts`; tsc/lint/vitest/build; restart dev; verifikasi browser 375/1280 (target konten pertama ≤ 220px; CSSOM CSSPageRule saat cetak); commit; PROJECT-STATE/PLAN v1.31/DECISIONS K-26 (F4 = 215×330; seed tanpa nama pribadi; printer hanya dialog sistem; nama menu).

Kontrak antar-agen: id slot toolbar `kanvas-toolbar-slot`; klip API baru di `lib/clipboard/appClipboard.ts`; token `KELAS.subNav*` & `HeaderView`/`SubNav` di `components/HeaderView.tsx`; kertas di `lib/cetak/kertas.ts`. Tidak ada library baru.
